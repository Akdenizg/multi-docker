name: Deploy MultiDocker
on:
  push:
    branches:
      - master # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t akdenizg/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true akdenizg/react-test npm test

      - run: docker build -t akdenizg/multi-client ./client
      - run: docker build -t akdenizg/multi-nginx ./nginx
      - run: docker build -t akdenizg/multi-server ./server
      - run: docker build -t akdenizg/multi-worker ./worker

      - run: docker push akdenizg/multi-client
      - run: docker push akdenizg/multi-nginx
      - run: docker push akdenizg/multi-server
      - run: docker push akdenizg/multi-worker
